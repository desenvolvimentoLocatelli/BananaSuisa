<UserControl x:Class="BananaSuisa.App.Controls.CompactActivityLogStrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <Border Padding="28,2,28,4"
            Background="{StaticResource WidgetBackgroundBrush}"
            BorderBrush="{StaticResource SidebarBorderBrush}"
            BorderThickness="0,1,0,0">
        <Border HorizontalAlignment="Stretch"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource WidgetBorderBrush}"
                BorderThickness="1"
                Padding="6,6"
                ClipToBounds="True">
            <TextBlock HorizontalAlignment="Stretch"
                       FontFamily="Consolas"
                       FontSize="11"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       LineHeight="16"
                       MaxHeight="32"
                       Text="{Binding LogText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="Wrap"
                       ToolTip="{Binding LogText, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Border>
    </Border>
</UserControl>
