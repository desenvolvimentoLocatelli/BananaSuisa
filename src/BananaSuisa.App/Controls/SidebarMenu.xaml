<UserControl x:Class="BananaSuisa.App.Controls.SidebarMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource SidebarBackgroundBrush}"
             BorderBrush="{StaticResource SidebarBorderBrush}" BorderThickness="0,0,1,0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Margin="20,24,20,32">
            <Image Source="pack://application:,,,/Assets/BananaSuisa.png" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,12,0" />
            <TextBlock Text="BananaSuisa" Foreground="{StaticResource TextHighlightBrush}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
        </StackPanel>
        
        <StackPanel Grid.Row="1">
            <Button Style="{StaticResource SidebarButtonStyle}" Content="Início" Command="{Binding NavigateCommand}" CommandParameter="Dashboard" />
            <Button Style="{StaticResource SidebarButtonStyle}" Content="Catálogo" Command="{Binding NavigateCommand}" CommandParameter="Catalog" />
            <Button Style="{StaticResource SidebarButtonStyle}" Content="Logs" Command="{Binding NavigateCommand}" CommandParameter="Logs" />
            <Button Style="{StaticResource SidebarButtonStyle}" Content="Configurações" Command="{Binding NavigateCommand}" CommandParameter="Settings" />
        </StackPanel>
    </Grid>
</UserControl>