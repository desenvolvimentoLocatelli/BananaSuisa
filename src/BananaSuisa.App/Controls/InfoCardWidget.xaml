<UserControl x:Class="BananaSuisa.App.Controls.InfoCardWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="root">
    <Border Padding="20" Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="12">
        <StackPanel>
            <TextBlock Style="{StaticResource SubHeaderTextStyle}" Text="{Binding CardTitle, ElementName=root}" Margin="0,0,0,12" />
            <ContentPresenter Content="{Binding CardContent, ElementName=root}" />
        </StackPanel>
    </Border>
</UserControl>