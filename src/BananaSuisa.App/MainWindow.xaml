<Window x:Class="BananaSuisa.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BananaSuisa.App"
        xmlns:controls="clr-namespace:BananaSuisa.App.Controls"
        xmlns:converters="clr-namespace:BananaSuisa.App.Converters"
        xmlns:behaviors="clr-namespace:BananaSuisa.App.Behaviors"
        Title="BananaSuisa .NET"
        Icon="pack://application:,,,/Assets/BananaSuisa.ico"
        Width="1200"
        Height="800"
        MinWidth="900"
        MinHeight="640"
        Background="{StaticResource AppBackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- Menu principal ou menu do fluxo Instalar -->
        <Grid Grid.Column="0">
            <controls:SidebarMenu Visibility="{Binding IsInstallMode, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <controls:InstallSidebarMenu Visibility="{Binding IsInstallMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>
        
        <!-- Área Direita -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Área de Trabalho Dinâmica -->
            <ContentControl x:Name="MainContentArea" Grid.Row="0" Content="{Binding CurrentView}" />

            <!-- Log de instalação junto ao rodapé (apenas no fluxo Instalar); scroll vertical/horizontal quando o texto excede a área -->
            <Border Grid.Row="1"
                    Visibility="{Binding IsInstallMode, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Background="{StaticResource WidgetBackgroundBrush}"
                    BorderBrush="{StaticResource SidebarBorderBrush}"
                    BorderThickness="0,1,0,0"
                    MinHeight="96"
                    MaxHeight="220">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" MinHeight="72" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Margin="14,8,14,4" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Text="Log de instalação" />
                    <TextBox Grid.Row="1"
                             Margin="12,0,12,10"
                             VerticalAlignment="Stretch"
                             MinHeight="72"
                             MaxHeight="180"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             behaviors:TextBoxWheelBehavior.ForwardOverflowToMainContentScrollViewer="True"
                             IsReadOnly="True"
                             Background="{StaticResource CardBackgroundBrush}"
                             BorderBrush="{StaticResource WidgetBorderBrush}"
                             BorderThickness="1"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             FontFamily="Consolas"
                             FontSize="11"
                             Padding="8"
                             TextWrapping="NoWrap"
                             Text="{Binding InstallActivityLog, Mode=OneWay}" />
                </Grid>
            </Border>

            <!-- Barra de status -->
            <controls:StatusFooter Grid.Row="2" StatusText="Pronto" ProgressValue="0" IsProgressVisible="False" RightText="v0.1.0-alpha" />

            <!-- Overlay de Carregamento Global -->
            <controls:LoadingOverlay Grid.RowSpan="3" Message="{Binding LoadingMessage}" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" />
        </Grid>
    </Grid>
</Window>