<UserControl x:Class="BananaSuisa.App.Views.InstallShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             VerticalAlignment="Stretch"
             HorizontalAlignment="Stretch"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="600">
    <!-- Grid em vez de ScrollViewer: o filho recebe altura limitada e pode usar linha * (lista ocupa o restante). -->
    <ContentControl Margin="0"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Content="{Binding InstallChildView}" />
</UserControl>
