<UserControl x:Class="BananaSuisa.App.Views.UwpProvisionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BananaSuisa.App.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="500">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource HeaderTextStyle}" Text="UWP / App Installer" />
            <TextBlock Margin="0,6,0,0" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" TextWrapping="Wrap"
                       Text="Consulta pacotes Microsoft.DesktopAppInstaller e Microsoft.WindowsStore (quando existir). A instalacao/reparo usa o mesmo bundle oficial do GitHub. O log aparece na barra inferior." />
        </StackPanel>

        <controls:InfoCardWidget Grid.Row="2" CardTitle="Status">
            <controls:InfoCardWidget.CardContent>
                <TextBlock TextWrapping="Wrap" Text="{Binding UwpProbeSummary}" />
            </controls:InfoCardWidget.CardContent>
        </controls:InfoCardWidget>

        <WrapPanel Grid.Row="4" Orientation="Horizontal">
            <Button Margin="0,0,10,10" Style="{StaticResource ToolbarActionButtonStyle}" MinWidth="160" Content="Verificar pacotes" Command="{Binding ProbeUwpCommand}" />
            <Button Margin="0,0,10,10" Style="{StaticResource ToolbarActionButtonStyle}" MinWidth="260" Content="Instalar / reparar App Installer" Command="{Binding InstallUwpBundleCommand}" />
        </WrapPanel>
    </Grid>
</UserControl>
