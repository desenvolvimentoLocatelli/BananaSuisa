<UserControl x:Class="BananaSuisa.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BananaSuisa.App.Controls"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="{StaticResource WorkspaceContentMargin}" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="28" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="28" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Style="{StaticResource HeaderTextStyle}" Text="{Binding Title}" />
                <TextBlock Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" TextWrapping="Wrap" Text="{Binding Subtitle}" />
            </StackPanel>

            <controls:InfoCardWidget Grid.Row="2" CardTitle="Bem-vindo">
                <controls:InfoCardWidget.CardContent>
                    <StackPanel>
                        <TextBlock TextWrapping="Wrap" LineHeight="22"
                                   Text="Use o menu à esquerda para navegar. O diagnóstico de runtime, caminhos do workspace e checagens detalhadas ficam em Logs, organizados em tabelas para leitura rápida." />
                        <TextBlock Margin="0,16,0,0" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" TextWrapping="Wrap"
                                   Text="Catálogo e Configurações receberão telas dedicadas conforme o app evoluir." />
                    </StackPanel>
                </controls:InfoCardWidget.CardContent>
            </controls:InfoCardWidget>

            <controls:InfoCardWidget Grid.Row="4" CardTitle="Resumo rápido">
                <controls:InfoCardWidget.CardContent>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="Workspace" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Column="1" Text="{Binding WorkspaceSummary}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="2" Text="Coleta" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" Text="{Binding GeneratedAt, StringFormat=Última coleta: {0}}" />
                    </Grid>
                </controls:InfoCardWidget.CardContent>
            </controls:InfoCardWidget>
        </Grid>
    </ScrollViewer>
</UserControl>
