<UserControl x:Class="BananaSuisa.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BananaSuisa.App.Controls"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Style="{StaticResource HeaderTextStyle}" Text="{Binding Title}" />
                <TextBlock Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" TextWrapping="Wrap" Text="{Binding Subtitle}" />
            </StackPanel>

            <controls:InfoCardWidget Grid.Row="2" CardTitle="Resumo do bootstrap">
                <controls:InfoCardWidget.CardContent>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Base directory" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding BaseDirectory}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="2" Text="Project root" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ProjectRoot}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="4" Text="Resources root" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ResourcesRoot}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="6" Text="Memory root" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding MemoryRoot}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="8" Text="Winget" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding WingetPath}" TextWrapping="Wrap" />
                    </Grid>
                </controls:InfoCardWidget.CardContent>
            </controls:InfoCardWidget>

            <controls:InfoCardWidget Grid.Row="4" CardTitle="Workspace inicializado">
                <controls:InfoCardWidget.CardContent>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="16" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Text="Resumo" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding WorkspaceSummary}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="2" Text="Data root" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DataRoot}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="4" Text="Config path" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ConfigPath}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="6" Text="Config source" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding ConfigurationSourcePath}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="8" Text="Config summary" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding ConfigurationSummary}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="10" Text="Catalog summary" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="10" Grid.Column="1" Text="{Binding CatalogSummary}" TextWrapping="Wrap" />

                        <ItemsControl Grid.Row="12" Grid.ColumnSpan="2" ItemsSource="{Binding WorkspaceItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,10" Padding="14" Background="{StaticResource WidgetBackgroundBrush}" BorderBrush="{StaticResource WidgetBorderBrush}" BorderThickness="1" CornerRadius="10">
                                        <StackPanel>
                                            <DockPanel>
                                                <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding Name}" />
                                                <Border DockPanel.Dock="Right" Padding="8,2" Background="{StaticResource AccentBrush}" CornerRadius="999">
                                                    <TextBlock FontSize="11" FontWeight="Bold" Text="{Binding StatusText}" />
                                                </Border>
                                            </DockPanel>
                                            <TextBlock Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Text="{Binding Detail}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </controls:InfoCardWidget.CardContent>
            </controls:InfoCardWidget>

            <controls:InfoCardWidget Grid.Row="6" CardTitle="Busca piloto da configuracao">
                <controls:InfoCardWidget.CardContent>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="16" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Text="Resumo" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SearchSummary}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="2" Text="Consulta" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SearchPreviewQuery}" TextWrapping="Wrap" />

                        <ItemsControl Grid.Row="4" Grid.ColumnSpan="2" ItemsSource="{Binding SearchPreviewItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,10" Padding="14" Background="{StaticResource WidgetBackgroundBrush}" BorderBrush="{StaticResource WidgetBorderBrush}" BorderThickness="1" CornerRadius="10">
                                        <StackPanel>
                                            <DockPanel>
                                                <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding Title}" />
                                                <Border DockPanel.Dock="Right" Padding="8,2" Background="{StaticResource AccentBrush}" CornerRadius="999">
                                                    <TextBlock FontSize="11" FontWeight="Bold" Text="{Binding Kind}" />
                                                </Border>
                                            </DockPanel>
                                            <TextBlock Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Text="{Binding Detail}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </controls:InfoCardWidget.CardContent>
            </controls:InfoCardWidget>

            <controls:InfoCardWidget Grid.Row="8" CardTitle="Busca piloto do catalogo">
                <controls:InfoCardWidget.CardContent>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="16" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Text="Resumo" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CatalogSearchSummary}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="2" Text="Catalogos" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CatalogSummary}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="4" Text="Consulta" Foreground="{StaticResource TextHighlightBrush}" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding CatalogSearchPreviewQuery}" TextWrapping="Wrap" />

                        <ItemsControl Grid.Row="6" Grid.ColumnSpan="2" ItemsSource="{Binding CatalogSearchPreviewItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,10" Padding="14" Background="{StaticResource WidgetBackgroundBrush}" BorderBrush="{StaticResource WidgetBorderBrush}" BorderThickness="1" CornerRadius="10">
                                        <StackPanel>
                                            <DockPanel>
                                                <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding Title}" />
                                                <Border DockPanel.Dock="Right" Padding="8,2" Background="{StaticResource AccentBrush}" CornerRadius="999">
                                                    <TextBlock FontSize="11" FontWeight="Bold" Text="{Binding Kind}" />
                                                </Border>
                                            </DockPanel>
                                            <TextBlock Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Text="{Binding Detail}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </controls:InfoCardWidget.CardContent>
            </controls:InfoCardWidget>

            <Grid Grid.Row="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="24" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Style="{StaticResource SubHeaderTextStyle}" Text="Checagens iniciais" />

                <ItemsControl Grid.Row="2" ItemsSource="{Binding Checks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,12" Padding="16" Background="{StaticResource WidgetBackgroundBrush}" BorderBrush="{StaticResource WidgetBorderBrush}" BorderThickness="1" CornerRadius="10">
                                <StackPanel>
                                    <DockPanel>
                                        <TextBlock FontSize="15" FontWeight="SemiBold" Text="{Binding Name}" />
                                        <Border DockPanel.Dock="Right" Padding="8,2" Background="{StaticResource AccentBrush}" CornerRadius="999">
                                            <TextBlock FontSize="11" FontWeight="Bold" Text="{Binding StatusText}" />
                                        </Border>
                                    </DockPanel>
                                    <TextBlock Margin="0,10,0,0" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Text="{Binding Detail}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Grid.Row="4" Foreground="#94A3B8" Text="{Binding GeneratedAt, StringFormat=Diagnostico gerado em {0}}" />
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>