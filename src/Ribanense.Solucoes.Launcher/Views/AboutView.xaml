<UserControl x:Class="Ribanense.Solucoes.Launcher.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Ribanense.Solucoes.Launcher.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AboutViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Sobre"
                       FontSize="22" FontWeight="SemiBold"
                       Foreground="{StaticResource Brush.Text}" />

            <Border Style="{StaticResource Card.Surface}" Margin="0,12,0,0">
                <StackPanel>
                    <TextBlock Text="Ribanense Soluções" Style="{StaticResource Text.Title}" />
                    <TextBlock Style="{StaticResource Text.Muted}" Margin="0,4,0,0">
                        <Run Text="Launcher " />
                        <Run Text="{Binding LauncherVersion, Mode=OneWay}" />
                        <Run Text=" — SDK " />
                        <Run Text="{Binding SdkVersion, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card.Surface}">
                <StackPanel>
                    <TextBlock Text="Configuração" Style="{StaticResource Text.Title}" />

                    <TextBlock Style="{StaticResource Text.Muted}" Margin="0,8,0,0" Text="Catálogo:" />
                    <TextBlock Style="{StaticResource Text.Body}" Text="{Binding CatalogUrl, Mode=OneWay}" TextWrapping="Wrap" />

                    <TextBlock Style="{StaticResource Text.Muted}" Margin="0,8,0,0" Text="Pasta de dados:" />
                    <TextBlock Style="{StaticResource Text.Body}" Text="{Binding DataRoot, Mode=OneWay}" TextWrapping="Wrap" />

                    <TextBlock Style="{StaticResource Text.Muted}" Margin="0,8,0,0" Text="Pasta de aplicativos:" />
                    <TextBlock Style="{StaticResource Text.Body}" Text="{Binding AplicativosRoot, Mode=OneWay}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <TextBlock Style="{StaticResource Text.Muted}" Margin="0,12,0,0">
                Configurável via variáveis de ambiente: RIBANENSE_CATALOG_URL, RIBANENSE_LAUNCHER_DATA, RIBANENSE_APLICATIVOS_ROOT.
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>
