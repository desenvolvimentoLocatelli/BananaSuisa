<UserControl x:Class="Ribanense.Solucoes.App.Farol.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Ribanense.Solucoes.App.Farol.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MapViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Faróis nesta rede" Style="{StaticResource Text.SectionTitle}" VerticalAlignment="Center" />
            <Button Content="Procurar agora"
                    Margin="16,0,0,0"
                    Style="{StaticResource Button.Base}"
                    Command="{Binding RefreshCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Margin="0,0,0,10"
                   Style="{StaticResource Text.Muted}"
                   Text="{Binding StatusMessage}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVis}}" />

        <Border Grid.Row="2"
                Style="{StaticResource Panel.Surface}"
                VerticalAlignment="Top"
                Visibility="{Binding HasPeers, Converter={StaticResource InverseBoolToVis}}">
            <TextBlock Style="{StaticResource Text.Muted}" Text="{Binding EmptyMessage}" />
        </Border>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding HasPeers, Converter={StaticResource BoolToVis}}">
            <ItemsControl ItemsSource="{Binding Peers}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:PeerCardViewModel}">
                        <Border Style="{StaticResource Panel.Surface}"
                                MinWidth="260"
                                MaxWidth="360"
                                Margin="0,0,12,12">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10"
                                             VerticalAlignment="Center"
                                             Margin="0,0,8,0"
                                             Fill="{Binding State, Converter={StaticResource PeerStateToBrush}}" />
                                    <TextBlock Text="{Binding DisplayName}"
                                               FontWeight="SemiBold"
                                               FontSize="14"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <TextBlock Margin="18,4,0,0"
                                           Style="{StaticResource Text.Muted}"
                                           Text="{Binding StateLabel}" />

                                <TextBlock Margin="18,2,0,0"
                                           Style="{StaticResource Text.Muted}"
                                           Text="{Binding LastSeenLabel, StringFormat=Visto {0}}" />

                                <TextBlock Margin="18,2,0,0"
                                           Style="{StaticResource Text.Muted}"
                                           Text="{Binding Address}" />

                                <Border Margin="18,10,0,0"
                                        Padding="10,8"
                                        CornerRadius="4"
                                        Background="{StaticResource Brush.Info.Bg}"
                                        BorderThickness="3,0,0,0"
                                        BorderBrush="{Binding HealthLevel, Converter={StaticResource HealthLevelToBrush}}">
                                    <TextBlock Text="{Binding HealthHeadline}" TextWrapping="Wrap" FontSize="12" />
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
