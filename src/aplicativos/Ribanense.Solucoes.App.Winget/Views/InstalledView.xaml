<UserControl x:Class="Ribanense.Solucoes.App.Winget.Views.InstalledView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Ribanense.Solucoes.App.Winget.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:InstalledViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource Brush.TextMuted}"
                       Text="{Binding StatusMessage}" />
            <Button Grid.Column="1"
                    Content="Atualizar lista"
                    Style="{StaticResource Button.Base}"
                    Command="{Binding RefreshCommand}" />
        </Grid>

        <DataGrid ItemsSource="{Binding Packages}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*" MinWidth="80" />
                <DataGridTextColumn Header="Id"   Binding="{Binding Id}"   Width="*" MinWidth="80" />
                <DataGridTextColumn Header="Versão" Binding="{Binding Version}" Width="*" MinWidth="80" />
                <DataGridTextColumn Header="Disponível" Binding="{Binding AvailableVersion}" Width="*" MinWidth="80" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" MinWidth="80" />
                <DataGridTemplateColumn Header="" Width="98" MinWidth="98" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Atualizar"
                                    Width="88"
                                    HorizontalAlignment="Center"
                                    Style="{StaticResource Button.Accent}"
                                    Margin="5,2"
                                    Command="{Binding UpgradeCommand}"
                                    Visibility="{Binding CanUpgrade, Converter={StaticResource BoolToVis}, ConverterParameter=Hidden}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="" Width="98" MinWidth="98" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remover"
                                    Width="88"
                                    HorizontalAlignment="Center"
                                    Style="{StaticResource Button.Danger}"
                                    Margin="5,2"
                                    Command="{Binding UninstallCommand}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
