<UserControl x:Class="Ribanense.Solucoes.App.Winget.Views.InstalledView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Ribanense.Solucoes.App.Winget.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:InstalledViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource Brush.TextMuted}"
                       Text="{Binding StatusMessage}" />
            <Button Grid.Column="1"
                    Content="Atualizar lista"
                    Style="{StaticResource Button.Base}"
                    Command="{Binding RefreshCommand}" />
        </Grid>

        <DataGrid ItemsSource="{Binding Packages}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*" MinWidth="140" />
                <DataGridTextColumn Header="Id"   Binding="{Binding Id}"   Width="2*" MinWidth="200" />
                <DataGridTextColumn Header="Versão" Binding="{Binding Version}" Width="Auto" MinWidth="80" />
                <DataGridTextColumn Header="Disponível" Binding="{Binding AvailableVersion}" Width="Auto" MinWidth="80" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="2*" MinWidth="120" />
                <DataGridTemplateColumn Header="" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Atualizar"
                                        Style="{StaticResource Button.Accent}"
                                        Margin="6,2,4,2"
                                        Command="{Binding UpgradeCommand}"
                                        Visibility="{Binding CanUpgrade, Converter={StaticResource BoolToVis}}" />
                                <Button Content="Remover"
                                        Style="{StaticResource Button.Danger}"
                                        Margin="4,2,6,2"
                                        Command="{Binding UninstallCommand}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
