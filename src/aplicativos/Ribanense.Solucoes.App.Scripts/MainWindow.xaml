<Window x:Class="Ribanense.Solucoes.App.Scripts.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Ribanense.Solucoes.App.Scripts.ViewModels"
        xmlns:b="clr-namespace:Ribanense.Solucoes.UI.Behaviors;assembly=Ribanense.Solucoes.UI"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        Title="Scripts — Ribanense Soluções"
        MinWidth="{StaticResource Breakpoint.MinWindowWidth}"
        MinHeight="{StaticResource Breakpoint.MinWindowHeight}"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Brush.Background}"
        b:WindowBreakpoints.Enabled="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Border}"
                BorderThickness="0,0,0,1"
                Padding="20,12">
            <TextBlock Text="{Binding ProductName}"
                       FontSize="16" FontWeight="SemiBold"
                       Foreground="{StaticResource Brush.Text}" />
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" MinWidth="200" MaxWidth="360" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource Brush.SidebarBg}"
                    BorderBrush="{StaticResource Brush.Border}"
                    BorderThickness="0,0,1,0">
                <ListBox Background="Transparent"
                         BorderThickness="0"
                         Padding="0,8"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ItemsSource="{Binding Scripts}"
                         SelectedItem="{Binding SelectedScript}"
                         ItemContainerStyle="{StaticResource ListBoxItem.Sidebar}">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ScriptListItemViewModel}">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Description}"
                                           Foreground="{StaticResource Brush.TextMuted}"
                                           FontSize="12"
                                           Margin="0,4,0,0"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Grid Grid.Column="1" Margin="20">
                <ContentControl Content="{Binding SelectedScript.Content}" />
            </Grid>
        </Grid>
    </Grid>
</Window>
